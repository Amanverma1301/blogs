<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Document</title>

  <!-- 🌟 Custom Free Fire CSS -->
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: url('https://wallpaperaccess.com/full/6382466.jpg') no-repeat center center fixed;
      background-size: cover;
      color: #fff;
    }

    .container {
background: transparent;    
  padding: 10px;
      border-radius: 12px;
      margin-top: 50px;
      box-shadow: 0 8px 70px rgba(255, 0, 0, 0.15);
    }

    h3 {
      color: #f1c40f;
      margin-bottom: 25px;
      text-shadow: 1px 1px #000;
    }

    input[type="text"] {
      width: 100%;
      padding: 12px;
      border-radius: 6px;
      border: none;
      font-size: 16px;
     background-color: gray;
      color: #fff;
    }

    .btn-primary {
      background-color: #f39c12;
      border: none;
      color: #000;
      padding: 10px 20px;
      border-radius: 5px;
      font-weight: bold;
      cursor: pointer;
    }

    .btn-primary:hover {
      background-color: #d35400;
    }

    .comment-box {
      background-color: rgba(255, 255, 255, 0.1);
      padding: 15px;
      border-radius: 10px;
      margin-top: 15px;
    }

    .comment-box img {
      border-radius: 50%;
      margin-right: 10px;
      vertical-align: middle;
    }

    .comment-author {
      font-weight: bold;
            color: #f1c40f;
      text-shadow: 1px 1px #000;

    }

    pre {
              font-weight: bold;

      white-space: pre-wrap;
      color: #000000;
      margin-left: 60px;
    }
  </style>
</head>

<body>
  <div class="container mt-3">
<h3>🔥 Comments</h3>
    <% if (locals.user) { %>
      <form action="/blog/comment/<%= blog._id %>" method="post">
        <div class="mb-3">
          <input
            type="text"
            class="form-control"
            name="content"
            placeholder="Enter your comment"
          />
        </div>
        <button class="btn btn-sm btn-primary" type="submit">Add</button>
      </form>
    <% } %>

    <div class="mt-3">
      <% comment.forEach(comment => { %>
        <div class="comment-box">
          <% if (comment.createdby) { %>
            <img src="<%= comment.createdby.coverimageurl%>" width="50px" alt="">
            <span class="comment-author"><%= comment.createdby.fullname%></span>
          <% } %>
          <pre><%= comment.content %></pre>
        </div>
      <% }) %>
    </div>
  </div>
</body>
</html>
